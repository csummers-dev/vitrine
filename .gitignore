*.db
*.bak
_old
rice-box.go
.idea/
/filebrowser
/filebrowser.exe
/dist
.venv

.DS_Store
node_modules

# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*
bin/
build/

# Vue distributable files
/frontend/dist/*
!/frontend/dist/.gitkeep

default.nix
Dockerfile.dev

docs/*.md
mockups/

# Local build/push helpers
build-and-push.sh
rc-build.sh

# RC / docker image tarballs (output of rc-build.sh, e.g. `docker save`)
# — large binaries that must never be committed (GitHub rejects >100 MB).
fb-rc.tar.gz
*.tar.gz
*.tgz
*.tar
